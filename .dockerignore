# Speeds up the build-context upload only — the Dockerfile COPYs specific
# files/dirs by name, so nothing here can accidentally break a COPY the way
# excluding nginx.conf once did (see Dockerfile comment).
.git
.claude
node_modules
